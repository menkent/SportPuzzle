image: trion/ng-cli

stages:
  - build
  - upload

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: build
  script:
    - rm ./package-lock.json
    - npm install

build_stage:
  stage: build
  environment: Stage
  only:
    - master
  script:
    - npm run toprod
  artifacts:
    paths:
      - dist/SportPuzzle/
